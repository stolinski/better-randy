# Stale Skill Sources

The Preset Pack defaults to `syntax`.
`export-video.ts` owns the `window.__gfxExport` seam and real export path.
Opaque output uses ProRes 422/H.264.
The designed kits resolve `item:N` identities.
