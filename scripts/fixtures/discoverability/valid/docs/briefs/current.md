# Current Brief

**Pack:** syntax

Preview at http://localhost:7263, add a `paper-grain` entry to `effects[]`, and use engine-default samples with per-motion `sound` overrides.

## What 'done' looks like

One Preset passes at native horizontal (3840x2160) and vertical (2160x3840) resolutions without orientation-specific siblings.

Pack-immunity is derived from `PACK_IMMUNE_PIPELINE_KEYS`.
