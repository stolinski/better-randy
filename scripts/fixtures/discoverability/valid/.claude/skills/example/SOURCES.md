# Current Skill Sources

`CompositionExportController` owns export orchestration, `Workspace.svelte` exposes the mounted callback, and `export-video.ts` provides encoding primitives.
The current ProRes lane emits 4444; ProRes 422 and H.264 lanes are future and unbuilt.
Engine-default samples and typed timeline identities replace retired protocols.
