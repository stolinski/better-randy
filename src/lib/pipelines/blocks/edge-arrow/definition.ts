import type { BlockPipelineDefinition } from '$lib/platform/pipelines/definition-types';

export const edgeArrowBlockDefinition = {
	type: 'edge-arrow'
} satisfies BlockPipelineDefinition<'edge-arrow'>;
