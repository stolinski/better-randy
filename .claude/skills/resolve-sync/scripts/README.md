# Project Script Dependencies

This workflow uses the repository-level Resolve bridge scripts named in `SKILL.md`. It has no skill-local executable copies; run the named paths from the repository root.
