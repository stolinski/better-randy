name: Quality

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check
        run: pnpm check

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

      - name: Structural tests
        run: pnpm test:structural

      - name: Planning audit
        run: pnpm audit:planning

      - name: Verify presets
        run: pnpm verify-presets

      - name: Build
        run: pnpm build

      - name: Install Chrome
        id: chrome
        uses: browser-actions/setup-chrome@v2

      - name: Start creator app
        run: pnpm dev --host 127.0.0.1 > /tmp/supers-vite.log 2>&1 &

      - name: Start flag-enabled Chrome
        run: >-
          "${{ steps.chrome.outputs.chrome-path }}"
          --headless=new
          --no-sandbox
          --disable-dev-shm-usage
          --enable-unsafe-webgpu
          --use-angle=swiftshader
          --enable-blink-features=CanvasDrawElement
          --remote-debugging-port=9223
          --user-data-dir=/tmp/supers-chrome
          about:blank > /tmp/supers-chrome.log 2>&1 &

      - name: Browser render test
        run: pnpm test:browser
